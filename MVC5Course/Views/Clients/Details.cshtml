@model MVC5Course.Models.Client
@{
    if (IsAjax)
    {
        Layout = "";
    }
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Client</h4>
    <hr />

    @Html.DisplayForModel()

    <h4>Client's Order List</h4>

    @{
        var orderList = ViewBag.OrderList as IEnumerable<Order>;
    }

    @*@Html.Action("Index", "Orders", new { clientId = Model.ClientId })*@

    @Html.Partial("OrderList", Model.Order.ToList(), new ViewDataDictionary(this.ViewData) { { "index", 1 } })

    @*@Html.DisplayFor(m => m.Order, "OrderList")*@


</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ClientId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
